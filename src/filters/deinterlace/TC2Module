module		deinterlace
name		"TCVP/filter/deinterlace"
version		0.1.0
tc2version	0.6.0
sources		deinterlace.c

TCVP {
	filter "filter/deinterlace" {
		new di_new
		packet DATA di_input
		probe di_probe
	}
}
