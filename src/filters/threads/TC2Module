module		threads
name		"TCVP/filter/threads"
version		0.1.0
tc2version	0.6.0
sources		threads.c
import		"stream" "new"
import		"stream" "new_pipe"
import		"stream" "close_pipe"

TCVP {
	filter "filter/parallel" {
		new thr_new
		packet DATA thr_input
		probe thr_probe
		flush thr_flush
	}
}
