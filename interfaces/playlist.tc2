symbol	"new"	playlist_t *(*%s)(conf_section *)
include
typedef struct playlist playlist_t;
struct playlist {
    int (*add)(playlist_t *, char **, int n, int pos);
    int (*remove)(playlist_t *, int start, int n);
    int (*get)(playlist_t *, char **, int start, int n);
    int (*count)(playlist_t *);
    int (*shuffle)(playlist_t *, int s, int n);
    void (*free)(playlist_t *);
    void *private;
};
